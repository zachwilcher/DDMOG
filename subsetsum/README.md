# What?
This package contains a collection of various implementations of the subset sum problem
variant that shows up when trying to determine valid DDMOGs. The Solver class implementations
are used in the ddm package by `ddmog_iterator.py`

