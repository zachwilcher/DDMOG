# What?
This directory contains various graphs and pngs of them which showed up
when searching computationally for DDMOGs.
In the `ddm` package, the `text` module contains functions for parsing
the various text files here.